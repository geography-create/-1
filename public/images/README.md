# 승기천 사진

이 폴더에 아래 두 파일을 넣으면 인트로 화면에서 실제 사진으로 표시됩니다.

- `seunggicheon-culvert.jpg` — 복개된 하천 구간 사진
- `seunggicheon-eco.jpg` — 생태하천으로 복원된 구간 사진

가로로 긴 사진(5:3 비율 권장)이 잘 어울리고, 웹 로딩 속도를 위해 1200px 폭 내외로
최적화해서 넣는 것을 권장합니다.
